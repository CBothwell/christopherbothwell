<!DOCTYPE html>
<html lang="en">
  {% render head, title: postTitle %}
<body class=''>
  <header>
  {% assign pageCollection = collections.page | sort: "data.priority" %}
  {% render menu, page: page, pageCollection: pageCollection %}
  </header>
  <main class='m-5'>
    <div class='max-w-2xl p-5 mx-auto mt-10 border border-gray-200 rounded-lg'>
      <div class='flex items-baseline mb-3 place-content-between'>
        <h1 class='text-2xl'>{{postTitle}}</h1>
        <div>
          <p>Posted: {{page.date | date: '%Y-%m-%d'}}</p>
        </div>
      </div>
      {% image postImage postImageAlt "(min-width: 30em) 50vw, 100vw" "mx-auto mb-5"%}
      {{ content }}
    </div>
  </main>
</body>
</html>
